{
  "name": "markdown-archive-site",
  "description": "A daisyUI-powered static site generator for Markdown archives.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:docs": "node scripts/build-docs.mjs",
    "build:site": "npm run build:docs && npm run build:css",
    "build:css": "tailwindcss -i ./scripts/site.css -o ./site/styles.css --minify"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.17",
    "tailwindcss": "^4.3.3"
  }
}
